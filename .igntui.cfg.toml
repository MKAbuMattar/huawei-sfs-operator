# .igntui.cfg.toml — generated by igntui; safe to commit.
# Re-running igntui in this directory restores this selection.

schema_version = 1
generated_at = "2026-05-11T13:02:06Z"
igntui_version = "0.1.1"

[selection]
templates = [
    "go",
    "helm",
    "kirby3",
    "visualstudiocode",
]
search_mode = "fuzzy"

[output]
path = ".gitignore"
preserve_user_edits = true
