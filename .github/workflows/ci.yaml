name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: latest
          args: --timeout=5m

      - name: Verify generated code is up-to-date
        run: |
          make manifests generate
          git diff --exit-code

  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: |
          go mod tidy
          make test

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: cover.out
          if-no-files-found: ignore

  e2e:
    name: E2E (kind)
    runs-on: ubuntu-latest
    # The kubebuilder-scaffold e2e deploys the operator into kind and checks
    # the manager pod runs healthy. The manager fails fast at startup when
    # HUAWEI_PROJECT_ID / HUAWEI_REGION are unset (correct behaviour), so
    # this job needs real-or-fake HuaweiCloud config injected via a CI
    # secret to be meaningful. Marked continue-on-error until that's wired.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/latest/kind-linux-$(go env GOARCH)
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind
          kind version

      - name: Run e2e tests
        run: |
          go mod tidy
          make test-e2e

  helm-lint:
    name: Helm chart lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: azure/setup-helm@v4
        with:
          version: v3.16.0

      - name: helm lint
        run: helm lint deploy/helm/sfs-operator

      - name: helm template (server-side dry-run)
        # Chart requires huawei.projectId + huawei.region — pass placeholder
        # values so the fail() guards don't trip during template rendering.
        run: |
          helm template sfs-operator deploy/helm/sfs-operator \
            --namespace sfs-operator-system \
            --set huawei.projectId=ci-test-project \
            --set huawei.region=ap-southeast-1 \
            --set huawei.credentials.source=secret \
            --set huawei.credentials.secretName=huawei-creds \
            --debug \
            > /tmp/rendered.yaml
          wc -l /tmp/rendered.yaml

  docker-build:
    name: Docker build (no push)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false

      - uses: docker/setup-buildx-action@v3

      - name: Build (verify only)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: sfs-operator:ci-${{ github.sha }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
